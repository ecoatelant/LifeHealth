# LifeHealth

Pour utiliser Git, il faut :
 
    - Pour rajouter une MàJ :
        (git add .)
        git commit -m ""
        git push origin master
        
    - Pour chercher les MàJ :
        git pull
